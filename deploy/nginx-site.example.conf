# Пример для Ubuntu + Nginx. Скопируйте в /etc/nginx/sites-available/ и подставьте домен + пути к сертификатам.
# certbot certonly --nginx -d example.ru -d www.example.ru
#
# SSR слушает 127.0.0.1:4000, API — 127.0.0.1:3000 (как в проекте сейчас).

# upstream ssr_backend { server 127.0.0.1:4000; keepalive 32; }
# upstream api_backend { server 127.0.0.1:3000; keepalive 8; }

# server {
#     listen 443 ssl http2;
#     server_name example.ru www.example.ru;
#
#     ssl_certificate     /etc/letsencrypt/live/example.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/example.ru/privkey.pem;
#
#     location /api/ {
#         proxy_pass         http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#     }
#
#     location / {
#         proxy_pass         http://127.0.0.1:4000;
#         proxy_http_version 1.1;
#         proxy_set_header   Host $host;
#         proxy_set_header   Upgrade $http_upgrade;
#         proxy_set_header   Connection "upgrade";
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#     }
# }
#
# server {
#     listen 80;
#     server_name example.ru www.example.ru;
#     return 301 https://$host$request_uri;
# }
